# Production runtime for the incremental pipeline.
# Heavy legacy embedding/matcher dependencies are intentionally excluded.
Flask==3.1.2
google-genai==1.56.0
playwright==1.57.0
pypdf==6.6.2
python-dotenv==1.2.1
